 

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    
          <a class="navbar-brand" href="/productos">
       MB HOMES</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/productos">Home</a>
        </li>
        {{#if user}}
        {{#if user.admin }}
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/realtimeproducts">Administrador</a>
        </li>
                {{/if}}
      {{/if}}
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/cart">Cart</a>
        </li>
        
        
  {{#if user}}
          {{#unless user.admin }}
                <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/chat">Chat</a>
        </li>
          {{/unless}}
        {{/if}}

             <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/profile">Profile</a>
        </li>
        
      
        <li class="nav-item">
        <form action="/logout" method="post">
    <button type="submit" class="nav-link active" aria-current="page">LogOut</button>
</form>
        </li>
        
      </ul>
      
    </div>
  </div>
</nav>
